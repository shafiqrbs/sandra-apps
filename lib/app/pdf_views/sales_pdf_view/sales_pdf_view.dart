import 'package:flutter/material.dart';

class SalesPdfView extends StatelessWidget {
  const SalesPdfView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
